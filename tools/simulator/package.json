{
  "name": "@browser2ide/simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/sendInspect.js",
  "types": "./dist/sendInspect.d.ts",
  "bin": {
    "browser2ide-simulator": "./dist/sendInspect.js"
  },
  "exports": {
    ".": {
      "types": "./dist/sendInspect.d.ts",
      "import": "./dist/sendInspect.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --filter @browser2ide/protocol build && pnpm --filter @browser2ide/bridge build && vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "send": "pnpm run build && node dist/sendInspect.js send"
  },
  "dependencies": {
    "@browser2ide/protocol": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@browser2ide/bridge": "workspace:*",
    "@types/ws": "^8.5.12"
  }
}
